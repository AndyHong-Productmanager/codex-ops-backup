# Official-channel verification

Pass. Opened first-party `omo.dev/docs`, which documents `npm i -g omo-ai@beta` for the standalone beta. Direct `npm view omo-ai@beta version dist-tags engines --json` returned beta `5.0.0-0.beta.40`, Node >=24, and a distinct beta tag. First-party LazyCodex distribution documents `npx lazycodex-ai install` for Codex.

