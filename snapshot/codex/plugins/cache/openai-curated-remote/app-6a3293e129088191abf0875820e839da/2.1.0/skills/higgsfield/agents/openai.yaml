interface:
  display_name: "Higgsfield Presets"
  short_description: "Find presets, workflows, and setup instructions"
  default_prompt: "Use $higgsfield to find and follow the named preset or command."
policy:
  allow_implicit_invocation: true
